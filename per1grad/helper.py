# module for loading dataset and some messy stuff.

def load_cifar():
    x_train, y_train, x_test, y_test = 1, 1, 1, 1
    return x_train, y_train, x_test, y_test

def load_mnist():
    x_train, y_train, x_test, y_test = 1, 1, 1, 1
    return x_train, y_train, x_test, y_test
